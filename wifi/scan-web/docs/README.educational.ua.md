# ESP32 Wi‑Fi Scan Web (Educational)

Навчальний приклад для **ESP32 (Arduino IDE)**, який демонструє:

* підключення до Wi‑Fi у режимі `WIFI_STA` (DHCP)
* сканування доступних Wi‑Fi точок **під HTTP‑запит**
* вивід результатів у браузер у вигляді HTML‑сторінки
* детальні відладочні повідомлення у Serial Monitor

Проєкт створений **з пізнавальною метою** — щоб зрозуміти, як реально працює Wi‑Fi, HTTP та памʼять в ESP32.

---

## 🔧 Можливості

* Підключення до існуючої Wi‑Fi мережі (STA)
* Отримання мережевих параметрів по DHCP
* Вбудований HTTP‑сервер (порт 80)
* Сканування Wi‑Fi **лише при відкритті сторінки**
* Вивід:

  * № мережі
  * SSID або `[hidden]`
  * BSSID (MAC точки доступу)
  * Channel
  * RSSI
  * Тип шифрування (OPEN / WPA2 / WPA3 тощо)
  * Кількість знайдених мереж
  * Час сканування
* HTML без рамок, з чергуванням фону рядків
* Відладка:

  * підключення до Wi‑Fi
  * параметри мережі
  * HTTP‑запити (IP клієнта)
  * час та результат сканування
  * використання heap‑памʼяті

---

## 🧠 Важливі принципи

* ❌ **Немає кешування** — кожен запит виконує реальне сканування
* ❌ **Немає фонового scan** — лише під HTTP‑запит
* ❌ **Немає monitor mode / sniffing**
* ✅ Лише та інформація, яку точки доступу передають **стандартно** (beacon frames)

Рівень доступу еквівалентний команді:

```
/interface wireless scan   (MikroTik)
```

---

## ⏱️ Час сканування

Параметр винесений у константу:

```cpp
const uint32_t SCAN_TIMEOUT_MS = 5000;
```

### Рекомендації:

* **3000–5000 мс** — оптимально (рекомендовано)
* < 2000 мс — можливі пропущені AP
* > 8000 мс — погіршення UX, паузи Wi‑Fi

---

## 🌐 Поведінка Wi‑Fi

* ESP32 має **один радіомодуль**
* Під час `WiFi.scanNetworks()`:

  * STA‑зʼєднання коротко призупиняється
  * після scan автоматично відновлюється

Це **нормальна та очікувана поведінка**.

---

## 📟 Serial Monitor (що побачиш)

### Підключення до Wi‑Fi

* статус підключення
* IP, gateway, mask, DNS
* RSSI
* помилки підключення (лише якщо виникають)

### HTTP‑запит

* номер запиту
* IP клієнта
* URI

### Сканування

* старт scan
* кількість мереж
* час scan
* heap до та після

---

## 📄 Веб‑сторінка

Відкрий у браузері:

```
http://<IP_ESP32>/
```

Сторінка формується **на льоту** та містить актуальні дані ефіру.

---

## 📌 Обмеження (свідомі)

* не показує Wi‑Fi клієнтів
* не читає пакети
* не використовує monitor mode
* не призначений для продакшен‑використання

Це **навчальний інструмент**, а не IDS / scanner.

---

## 🧪 Для експериментів

Рекомендовано:

* кілька refresh сторінки (перевірка стабільності heap)
* відкриття з кількох пристроїв
* зміна `SCAN_TIMEOUT_MS`
* порівняння результатів з MikroTik

---

## ✅ Підсумок

Цей скетч — **еталонний навчальний приклад**:

* чиста логіка
* передбачувана поведінка
* повна прозорість через логи

Рекомендується як база для:

* розуміння Wi‑Fi в ESP32
* власних експериментів
* подальшого розширення функціоналу
